
public class Player {
	int color;
	int points;
	
	//1 is white
	//2 is black
	
	Player(int color){
		this.color = color;
		this.points = 0;
	}
	
	//returns a string in chess-diction to move
	//this may be there the AI is, for now
	//construct it to request for a string
	//have a scanner handle the input
	//and then re-output
	String giveAction(){
		
		return "FALSE";
	}
}
